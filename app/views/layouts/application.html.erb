<!DOCTYPE html>
<html class="h-full">
  <head>
    <title>Simpleshop</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta charset="UTF-8">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>
  <body>
    <%= render "navbar/navbar" %>
    <main class="container px-4 mt-6 mx-auto grid md:grid-cols-3 sm:grid-cols-2 gap-6 xl:gap-x-8 justify-center">
      <%= yield :main_item %>   
      <%= render "shared/top" %>
    </main>
    <%= yield %>
  </body>
</html>